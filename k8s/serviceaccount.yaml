apiVersion: v1
kind: ServiceAccount
metadata:
  name: books-api
  namespace: books-api
  annotations:
    # IRSA: bind this SA to an IAM role (e.g. for Amazon Managed Prometheus
    # remote-write, or reading DB creds from Secrets Manager). Fill in per account.
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/books-api
