# EXAMPLE ONLY — do not commit real secrets. In a real cluster, source this from
# AWS Secrets Manager via the External Secrets Operator or the Secrets Store CSI
# driver, and delete this file.
apiVersion: v1
kind: Secret
metadata:
  name: books-api-secrets
  namespace: books-api
type: Opaque
stringData:
  DATABASE_URL: postgresql+asyncpg://books:CHANGE_ME@books-db.example.rds.amazonaws.com:5432/books
